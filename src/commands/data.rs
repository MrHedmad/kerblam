use std::path::PathBuf;

use anyhow::Result;

use crate::options::KerblamTomlOptions;

pub fn print_data_status(config: KerblamTomlOptions, root_dir: PathBuf) -> Result<()> {
    todo!()
}

pub fn cleanup_data(config: KerblamTomlOptions, root_dir: PathBuf) -> Result<()> {
    todo!()
}

pub fn package_data(
    config: KerblamTomlOptions,
    root_dir: PathBuf,
    output_file: PathBuf,
) -> Result<()> {
    todo!()
}
